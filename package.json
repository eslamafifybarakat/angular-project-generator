{
  "name": "angular-project-generator",
  "version": "0.1.0",
  "description": "Configure an Angular project — architecture, rendering, theme, languages, environments and tooling — then generate it.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.22.3",
    "npm": ">=10.9.0"
  },
  "scripts": {
    "ng": "ng",
    "start": "node scripts/dev.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:4200": "node scripts/dev.mjs",
    "dev:4300": "node scripts/dev.mjs",
    "start:staging": "node scripts/dev.mjs --configuration staging",
    "start:uat": "node scripts/dev.mjs --configuration uat",
    "start:live": "node scripts/dev.mjs --configuration production",
    "watch": "ng build --watch --configuration development",
    "clean": "node scripts/clean.mjs",
    "clean:dist": "node scripts/clean.mjs dist",
    "clean:cache": "node scripts/clean.mjs cache",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "generate:icons": "node scripts/generate-icons.mjs",
    "generate:og": "node scripts/generate-og-images.mjs",
    "generate:csp-hashes": "node scripts/generate-csp-hashes.mjs",
    "generate:audit-pdf": "node scripts/generate-audit-pdf.mjs",
    "build": "node scripts/with-build-lock.mjs \"ng build --configuration production && npm run generate:sitemap && npm run generate:csp-hashes\"",
    "build:dev": "node scripts/with-build-lock.mjs \"ng build --configuration development && npm run generate:sitemap\"",
    "build:dev-api": "node scripts/with-build-lock.mjs \"ng build --configuration dev && npm run generate:sitemap\"",
    "build:staging": "node scripts/with-build-lock.mjs \"ng build --configuration staging && npm run generate:sitemap\"",
    "build:uat": "node scripts/with-build-lock.mjs \"ng build --configuration uat && npm run generate:sitemap\"",
    "build:live": "node scripts/with-build-lock.mjs \"ng build --configuration production && npm run generate:sitemap && npm run generate:csp-hashes\"",
    "build:ssr": "npm run build",
    "build:clean": "npm run clean && npm run build",
    "build:clean:dev": "npm run clean && npm run build:dev",
    "rebuild": "npm run clean && npm run build",
    "vercel-build": "node scripts/vercel-build.mjs",
    "serve:ssr": "node dist/angular-project-generator/server/server.mjs",
    "serve:ssr:angular-project-generator": "node dist/angular-project-generator/server/server.mjs",
    "serve:ssr:live": "node scripts/serve-ssr.mjs --port 4000",
    "serve:ssr:nonlive": "node scripts/serve-ssr.mjs --port 5000",
    "stop:dev": "node scripts/stop-dev-server.mjs",
    "ssr:dev": "npm run stop:dev && npm run build:dev && npm run serve:ssr:nonlive",
    "ssr:dev-api": "npm run stop:dev && npm run build:dev-api && npm run serve:ssr:nonlive",
    "ssr:staging": "npm run stop:dev && npm run build:staging && npm run serve:ssr:nonlive",
    "ssr:uat": "npm run stop:dev && npm run build:uat && npm run serve:ssr:nonlive",
    "ssr:live": "npm run stop:dev && npm run build:live && npm run serve:ssr:live",
    "ssr:clean": "npm run clean && npm run ssr:live",
    "ssr:clean:dev": "npm run clean && npm run ssr:dev",
    "lint": "ng lint",
    "type-check": "tsc -p tsconfig.app.json --noEmit",
    "test": "ng test",
    "lighthouse": "node scripts/lighthouse.mjs"
  },
  "dependencies": {
    "@angular/common": "^22.1.5",
    "@angular/compiler": "^22.1.5",
    "@angular/core": "^22.1.5",
    "@angular/forms": "^22.1.5",
    "@angular/platform-browser": "^22.1.5",
    "@angular/platform-server": "^22.1.5",
    "@angular/router": "^22.1.5",
    "@angular/service-worker": "^22.1.5",
    "@angular/ssr": "^22.1.7",
    "express": "^5.2.1",
    "jszip": "^3.10.1",
    "rxjs": "^7.8.2",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@angular/build": "^22.1.7",
    "@angular/cli": "^22.1.7",
    "@angular/compiler-cli": "^22.1.5",
    "@eslint/js": "^10.0.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.4.1",
    "angular-eslint": "^22.2.0",
    "chrome-launcher": "^1.2.1",
    "eslint": "^10.10.0",
    "jsdom": "^30.0.1",
    "lighthouse": "^13.4.1",
    "prettier": "^3.9.6",
    "sharp": "^0.35.4",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.69.0",
    "vitest": "^4.0.8"
  }
}
