# Build output
/dist
/out-tsc
/tmp
/.angular/cache
.build.lock

# Dependencies
/node_modules
npm-debug.log
yarn-error.log

# Environment and secrets — see README §"Secrets policy"
.env
.env.*
!.env.local.example

# Vercel account link — never commit; created locally by `vercel link`
.vercel

# Editors
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Local Claude Code session artifacts — machine/session-specific, never shared
/.claude_key_mapping.json
/.claude/scheduled_tasks.lock

# Generated reports
/LIGHTHOUSE.md
/AUDIT.pdf
